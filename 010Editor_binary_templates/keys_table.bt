LittleEndian();

typedef byte int8;
typedef ubyte uint8;

struct KeyMapping {
	uint16 keyLength;
	char key[keyLength];
};

struct KeysTable {
	uint8 config;
	uint16 count;
	KeyMapping mappings[count]<optimize=false>;
};

KeysTable keysTable;
